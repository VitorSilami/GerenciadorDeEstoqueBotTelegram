# Eos Cafés Especiais — Dashboard (React)

Dev commands:

- npm install
- npm run dev (proxy /api to Flask at :5000)
- npm run build (output to client/dist)

Deploy: Build the client, then run `python main.py`. Flask will serve client/dist at `/`.
